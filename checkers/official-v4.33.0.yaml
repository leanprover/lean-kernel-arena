version: "v4.33.0-rc2"
description: |
  The official Lean 4 kernel checker implementation.
dir: official-nightly
build: |
  echo leanprover/lean4:v4.33.0-rc2 > lean-toolchain
  lake build
run: .lake/build/bin/kernel "$IN"
