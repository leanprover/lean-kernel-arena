version: "2026-08-01"
description: |
  The official Lean 4 kernel checker implementation (nightly release).
dir: official
build: |
  echo leanprover/lean4:nightly-2026-08-01 > lean-toolchain
  lake build
run: .lake/build/bin/kernel "$IN"
